require 'spec_helper'

describe SpreeLastAddress do
  context ".find_last_used_addresses" do
    before do
      # TODO
      # create past order
      # create new order
    end

    it "return old addresses when matched" do
    end

    it "return current addresses when not matched" do
    end
  end
end
